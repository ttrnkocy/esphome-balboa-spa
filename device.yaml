# this is just a simple test device setup that's used to compile and test the examples

esphome:
    name: balboa_spa_test
    build_path: .build/balboa_spa_test

esp32:
  board: lolin_s2_mini
  framework: 
    type: arduino